import { RecaptchaResponse } from '../interfaces';
declare global {
    namespace Express {
        interface Request {
            recaptcha?: RecaptchaResponse;
        }
        interface Response {
            recaptcha?: string;
        }
    }
}
